# 大模型环境变量配置示例
# ─────────────────────────────────────────────
# 部署平台（如 bonto.dev）配置大模型时，直接填以下四个环境变量即可，
# 无需把 llm.config.json 入库；server.js 已支持并优先读取环境变量。
# 复制本文件为 .env 仅供本地开发，.env 本身已被 .gitignore 忽略，不会入库。
#
# 变量说明（与 llm.config.json 的 base/key/model/temperature 一一对应）：
#   LLM_API_BASE     OpenAI 兼容的 /v1 端点
#   LLM_API_KEY      服务商密钥（sk-xxx），绝不写入仓库
#   LLM_MODEL        模型 ID
#   LLM_TEMPERATURE  采样温度

LLM_API_BASE=https://api.siliconflow.cn/v1
LLM_API_KEY=sk-你的密钥
LLM_MODEL=deepseek-ai/DeepSeek-R1-0528-Qwen3-8B
LLM_TEMPERATURE=0.7
